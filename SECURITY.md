# Security policy

WhisperDeck runs everything locally. Report anything that makes it touch
the network outside the model download, or that executes model-controlled
text as code.

- Use GitHub private vulnerability reporting on this repo.
- Please include the version from `whisperdeck.__version__`.
